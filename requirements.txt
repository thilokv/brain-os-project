fastapi>=0.110.0
uvicorn>=0.27.0
langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
chromadb>=0.4.22
pydantic>=2.6.0
pydantic-settings>=2.4.0
python-dotenv>=1.0.0
slack-sdk>=3.27.0
anthropic>=0.18.0
httpx>=0.27.0

# PostgreSQL (Phase 2A foundation -- see PHASE2_COMMERCIAL_ARCHITECTURE.md;
# not used by /brain-os/* or the original SQLite schema).
psycopg2-binary>=2.9.9

# Alembic (Phase 2A.2 -- versioned Postgres migrations, hand-written SQL).
# Alembic depends on SQLAlchemy internally for its `op.*` directives, but
# the application does NOT adopt the SQLAlchemy ORM anywhere: migrations
# are written with op.execute(<raw SQL>), and the runtime repository
# layer (a later milestone) uses psycopg2 with parameterized SQL, the
# same discipline as the existing SQLite repository.
alembic>=1.13.0

# testing
pytest>=8.0.0